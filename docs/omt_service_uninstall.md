## omt service uninstall

Unload the LaunchAgent and remove its plist

### Synopsis

Unload the job and delete its plist. Logs in ~/Library/Logs are left in place.

```
omt service uninstall [flags]
```

### Options

```
  -h, --help   help for uninstall
```

### Options inherited from parent commands

```
      --binary string   path to the omt executable to run (default: the running one)
  -d, --debug           log raw HTTP responses from the OAuth2 endpoints
  -v, --verbose         increase verbosity
```

### SEE ALSO

* [omt service](omt_service.md)	 - Manage the omt LaunchAgent

